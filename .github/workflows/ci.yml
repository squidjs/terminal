name: CI
on:
  push:
    branches:
      - master
      - canary
    pull_request:
jobs:
  ci_linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install
        run: yarn
      - name: Rebuild dep
        run: yarn rebuild
      - name: Build
        run: yarn deploy:linux
